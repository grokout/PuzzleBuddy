using UnityEngine;
using System;
using System.Collections;
using System.Collections.Generic;

static public class JsonUtils
{/*
    static public bool GetFieldAndCheckForChange(JSONObject json, string key, ref float value)
    {
        float temp = 0;
        if (json.GetField(ref temp, key))
        {
            if ( temp != value)
            {
                value = temp;
                return true;
            }
        }
        return false;
    }

    static public bool GetFieldAndCheckForChange(JSONObject json, string key, ref int value)
    {
        int temp = 0;
        if (json.GetField(ref temp, key))
        {
            if (temp != value)
            {
                value = temp;
                return true;
            }
        }
        return false;
    }

    static public bool GetFieldAndCheckForChange(JSONObject json, string key, ref string value)
    {
        string temp = "";
        if (json.GetField(ref temp, key))
        {
            if (temp != value)
            {
                value = temp;
                return true;
            }
        }
        return false;
    }*/
}
